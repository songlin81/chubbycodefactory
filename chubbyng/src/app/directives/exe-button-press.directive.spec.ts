import { ExeButtonPressDirective } from './exe-button-press.directive';

describe('ExeButtonPressDirective', () => {
  it('should create an instance', () => {
    const directive = new ExeButtonPressDirective();
    expect(directive).toBeTruthy();
  });
});
