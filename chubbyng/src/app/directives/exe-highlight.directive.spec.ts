import { ExeHighlightDirective } from './exe-highlight.directive';

describe('ExeHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ExeHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
